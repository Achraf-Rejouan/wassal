# Local development values ONLY, committed deliberately so `docker compose up` works from a
# cold clone (NFR-007). These are not secrets and must never reach a network — the hosted
# overlay generates its own credentials at deploy time and shares nothing with this file.
POSTGRES_DB=wassal
POSTGRES_USER=wassal
POSTGRES_PASSWORD=wassal
